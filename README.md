🤖 هوش مصنوعی: سیستم دسته‌بندی قصد و چت‌بات پشتیبان (مسیر ۸)
پروژه نهایی درس هوش مصنوعی | دانشگاه صنعتی خواجه نصیرالدین طوسی

این پروژه شامل طراحی و پیاده‌سازی یک سیستم کامل پردازش زبان طبیعی (NLP) است که با استفاده از دیتاست تخصصی Bitext، توانایی تشخیص ۲۷ نوع قصد (Intent) مختلف مشتریان و استخراج موجودیت‌های کلیدی را دارد.

📂 ساختار درختی و ماژولار پروژه
Plaintext
.
├── data/
│   ├── raw/                # دیتاست اصلی Bitext_v11
│   └── processed/          # داده‌های تمیز شده و تفکیک شده (Train/Val/Test)
├── notebooks/
│   ├── 01_EDA.ipynb        # فاز ۱: تحلیل بصری و آماری داده‌ها
│   └── 02_Modeling.ipynb   # فاز ۲: آموزش مدل BERT و مقایسه‌ها
├── src/
│   ├── preprocessing/      # پاک‌سازی، نرمال‌سازی و توکن‌بندی
│   ├── models/             # معماری Baseline و مدل پیشرفته BERT
│   ├── training/           # موتور آموزش، Loss Function و Optimizer
│   └── inference/          # استخراج موجودیت و تولید پاسخ هوشمند
├── models/                 # محل ذخیره وزن‌های مدل (Best Weights)
├── results/
│   ├── charts/             # نمودارهای EDA و منحنی‌های آموزش
│   └── metrics/            # گزارش‌های عملکرد (Classification Report)
├── app.py                  # اجرای رابط کاربری گرافیکی (Gradio)
└── requirements.txt        # پیش‌نیازهای پایتونی
📝 خلاصه فاز ۱: تحلیل و زیرساخت (Foundation)
در این فاز تمرکز بر شناخت داده‌ها و آماده‌سازی مسیر برای مدل‌های عمیق بود:

تحلیل اکتشافی (EDA): رسم ۶ نمودار تحلیلی برای بررسی توازن کلاس‌ها (Label Imbalance) و توزیع طول جملات.

پیش‌پردازش تخصصی: پیاده‌سازی خط لوله تمیزکاری شامل حذف علائم نگارشی، کلمات توقف (Stopwords) و ریشه‌یابی (Lemmatization).

مدل پایه (Baseline): پیاده‌سازی مدل Logistic Regression به همراه TF-IDF که به دقت اولیه حدود ۸۲٪ دست یافت.

استراتژی ارزیابی: تقسیم‌بندی داده‌ها به روش Stratified (۷۰٪ آموزش، ۱۵٪ تست، ۱۵٪ اعتبارسنجی).

🛠 خلاصه فاز ۲: پیاده‌سازی عمیق و توسعه (Deep Learning)
در این فاز، سیستم از یک مدل ساده به یک هوش مصنوعی مدرن تبدیل شد:

معماری مدل: استفاده از مدل پیش‌آموزش‌دیده BERT (نسخه bert-base-uncased) و Fine-tuning آن روی داده‌های Bitext.

فرآیند آموزش: بهره‌گیری از بهینه‌ساز AdamW، تکنیک Gradient Clipping و سیستم Model Checkpointing برای ذخیره بهترین نسخه مدل.

استخراج موجودیت (NER): طراحی منطق استخراج شماره سفارش و کدهای پیگیری از دل متن کاربر با استفاده از Regex و پارسرهای قاعده‌مند.

رابط کاربری (Demo): توسعه یک محیط چت تعاملی با Gradio که به کاربر اجازه می‌دهد به صورت زنده با مدل در تعامل باشد.

📊 نتایج ارزیابی نهایی
مدل نهایی BERT بر روی داده‌های آزمون به نتایج خیره‌کننده‌ای دست یافت:

Weighted F1-Score: ~۹۶٪

تعداد کلاس‌ها: ۲۷ قصد (از جمله خرید، مرجوعی، پیگیری سفارش و غیره)

تحلیل خطا: بررسی ماتریس اغتشاش (Confusion Matrix) نشان داد که مدل در تشخیص قصدهای مشابه (مانند تفاوت لغو سفارش و مرجوعی) بسیار دقیق عمل می‌کند.

🛠 نحوه نصب و اجرا
۱. مخزن را کلون کنید:

Bash
git clone https://github.com/username/project-name.git
۲. کتابخانه‌ها را نصب کنید:

Bash
pip install -r requirements.txt
۳. برنامه را اجرا کنید:

Bash
python app.py
👥 اعضای تیم و مشارکت
[نام شما]: پیاده‌سازی مدل BERT، پیش‌پردازش و گزارش‌نویسی.

شماره دانشجویی: [شماره شما]
